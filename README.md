# Change the color
#9E182B (the wine red) to whatever color fits your project
# The hover effect triggers on every <a> and <button> automatically 
if you want it on other elements too, just add data-hover as an attribute to any HTML element like <div data-hover>
